# 推論パイプライン（フェーズ 1）

このノートは GPU を使わず、生成の「言葉」を固定するためのもの。実測は `Qwen/Qwen2.5-7B-Instruct` の **tokenizer のみ**（`scripts/tokenize_demo.py`）。

```text
Prompt
  → chat template（role 付きの文字列）
  → tokenize（token ID 列）
  → forward で logits（ここから先はフェーズ 2 以降、GPU）
  → sampling（temperature / top-p、1 トークン）
  → detokenize
  → EOS まで繰り返し
```

## BPE / サブワード

モデルの入力単位は「単語」でも「文字」でもなく **token ID** である。

**BPE**（Byte Pair Encoding）は、コーパス上でよく隣り合うバイト列を何度も結合して語彙を作るアルゴリズムである。実行時に「頻度を見て方式を切り替える」のではない。学習済みの **固定マージ規則** で文字列を ID に切る。頻度が出てくるのは語彙を作ったときだけ。

Qwen はバイトレベル寄りの BPE で、語彙は約 15 万。同じ文でも、よく出る列は 1 トークンにまとまり、珍しい列は細かく割れる。日本語は UTF-8 の複数バイトがトークン境界とずれることがある。実測では「ローカル」の「ー」がバイト途中で割れ、decode すると欠ける断片が出た。これはバグというより、サブワードが文字境界を保証しない例。

実測（`add_special_tokens=False`）:

| 文 | 文字数 | トークン数 |
| --- | --- | --- |
| `WSLからローカルLLMを動かしたい。` | 19 | 14 |
| `Summarize why quantization helps 8GB VRAM.` | 42 | 13 |

英語は空白付きのサブワード（`Ġwhy` のように前空白をトークンに含める）になりやすい。日本語は文字数よりトークン数が少なくなることも、逆にバイト割れで多くなることもある。

### raw と decode

モデルに渡るのは常に **整数 ID**。例: `53931`。

`results/01-tokenize.txt` の `'ãģĭãĤī'` は、tokenizer が UTF-8 バイトを latin-1 風に表示した内部表記である。同じ ID を `decode` すると `'から'` になる。表示が汚いのであって、モデルが文字化け文字列を学習しているわけではない。

- **token（raw 表示）**: デバッグ用のバイト列見え
- **decode**: 人が読む UTF-8
- **ID**: 実際の入力

### 混同: Word2vec と語彙 15 万

Word2vec は単語単位なので、語彙に無い表層はベクトルを返せない（OOV）。BPE は未知の入力を短い断片、最終的にはバイトまで割る。だからこの tokenizer の `unk` は `None`。語彙外でも **必ず ID 列になる**。

15 万は単語ベクトルの数百万より少なく見えるが、**サブワード語彙としては大きい**（Llama 系は 3 万台、GPT-2 は約 5 万）。多言語は「全単語を載せる」のではなく、言語をまたいだ共有サブワードでカバーする。漢字 1 字が 1 トークンになることも、珍しい列がバイトに割れることもある。

## 特殊トークン

この tokenizer では:

- BOS: なし（`bos_token` は `None`）
- EOS: `<|im_end|>`（id 151645）
- PAD: `<|endoftext|>`（id 151643）
- チャット区切り: `<|im_start|>`（id 151644）と role 名（`system` / `user` / `assistant`）

生成側は EOS を見たら止める。PAD はバッチの長さ合わせ用で、通常の 1 本の生成では主役ではない。

## chat template

同じユーザ文でも、テンプレートを付けると **まったく別の ID 列** になる。Instruct モデルは「user / assistant の型」で学習しているので、生文だけ渡すと品質が落ちやすい。

実測:

- 日本語: raw 14 トークン → template 後 **43** トークン（同じではない）
- 英語: raw 13 トークン → template 後 **42** トークン

Qwen のテンプレは system 文（「You are Qwen...」）と `<|im_start|>assistant` の生成開始までを先に載せる。API の `messages` はこのテンプレに落とされてから tokenize される、と考えてよい。

## temperature と top-p

**forward**（順伝播）は、入力の token ID を Transformer の層に通し、次トークン用のスコアを出す計算である。このフェーズでは走らせない。

**logits** はその生スコアで、長さは `vocab_size`（約 15 万）。まだ確率ではない。**softmax** で合計 1 の分布にしてから、1 つの ID を選ぶ。

- **temperature**: 分布をならす／尖らせる。0 に近いほど最大確率のトークンばかり。高いほど多様な続き。
- **top-p**: 確率の高い順に足して、累積が p を超える集合からだけサンプルする。

1 ステップで選ぶのは **1 トークン**。文字列をまとめて出しているように見えるのは、クライアントが detokenize して繋いでいるだけ。

## 量子化（なぜ 7B が 8GB VRAM に載るか）

パラメータ数 × 1 要素のバイト数が、だいたいの重みサイズ。

- 7B × FP16（2 バイト）≈ **14 GB** → 8GB GPU にはそのまま載らない
- 7B × 4bit 相当（Q4）≈ **3.5–5 GB** 前後 → 8GB に載り、KV cache の余裕も残る

量子化は重みの精度を落とす代わりに容量と帯域を減らす。品質は落ちうるが、ローカルではこのトレードオフが前提。このマシンの主戦力を Q4_K_M にした理由でもある。

VRAM を食うのは重みだけではない。次の KV cache が残る。

## KV cache

Transformer の各層は、これまでのトークンの Key / Value を参照する。生成のたびに過去を全部再計算せず、**既に計算した K/V を GPU メモリに残す**。これが KV cache。

入力が長いほど（コンテキスト長）、層数 × 系列長 × ヘッド次元でキャッシュが増える。だから `-c 2048` と `-c 8192` で VRAM が変わる（フェーズ 3 で実測する）。

混同ポイント: **ブラウザやユーザ PC に置く HTTP cache ではない**。次のユーザ入力を予測する仕組みでもない。モデル内部の attention 用に、次の **生成トークン** を安く計算するためのもの。

## GGUF

**GGUF** は、量子化済み重みとメタデータ（語彙、アーキテクチャ名など）をまとめたファイル形式である。

役割の分担:

- **safetensors**: Hugging Face 上の学習・変換に近い重み。このフェーズの tokenizer 取得元もこの系統
- **GGUF**: llama.cpp / Ollama が読む実行用。Q4 などはここに入っていることが多い

中身のバイナリ解剖はしない。GPU 側の用語（llama.cpp / CUDA / `-ngl` / `.so`）は [`learn-gpu-stack.html`](learn-gpu-stack.html)。
